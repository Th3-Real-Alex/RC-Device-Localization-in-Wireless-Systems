%% openImpactOfInvalidSync
% Opens the "Impact of invalid synchronization between anchors" page and closes "mainContainer"
function openImpactOfInvalidSync(mainContainer)
    impactOfInvalidSync();
    pause(0.5);
    close(mainContainer);
end